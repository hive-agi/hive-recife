{:project-id  "hive-recife"
 :description "A Clojure wrapper around recife (Clojure -> TLA+/TLC model checker): a normalized ModelCheckResult (:ok/:fail/:skip) + counterexample traces for safety/liveness verification of stateful protocols."
 :type        :library}
